// Typed API client — empty shell for scaffold phase.
// Will be replaced with real fetch calls during API integration (PRDs 7-9).

const API_URL = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000";

export { API_URL };
