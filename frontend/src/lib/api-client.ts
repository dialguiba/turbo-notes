// Typed API client — all API calls go through here.
// Feature PRDs (3-6) add real fetch calls as screens are built.

const API_URL = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000";

export { API_URL };
